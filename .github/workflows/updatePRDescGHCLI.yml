name: Update PR Description GH CLI
on:
  pull_request:
    types:
      - ready_for_review
jobs:
  track_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Update PR Description
        run: |
          BODY="$(gh api /repos/$OWNER/$REPO/pulls/$PR_NUMBER | jq -r '.body')"
          echo 'BODY='$BODY
        env:
          GITHUB_TOKEN: ${{ secrets.YOUR_TOKEN }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ github.event.repository.name }}
          PR_NUMBER: ${{ github.event.number }}
      # - run: |
      #    gh issue create --title "Issue report" --body "the body" --repo $GITHUB_REPOSITORY
      #  env:
      #    GITHUB_TOKEN: ${{ secrets.YOUR_TOKEN }}
        